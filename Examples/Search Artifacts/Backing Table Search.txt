Searching backing tables is much simpler than transactions, since backing table elements are much simpler.

$A (if applicable) limits the return to only entries for which inAccount() is true.
$a (if applicable) limits the return to only entries for which inAccount() is false.